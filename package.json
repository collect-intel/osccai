{
  "name": "osccai-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "consensus-service": "pnpm --filter osccai-consensus-service",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "web": "pnpm --filter osccai-web"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
