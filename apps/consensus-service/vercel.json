{
  "functions": {
    "api/update-gac-scores.py": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "crons": [
    {
      "path": "/api/update-gac-scores.py",
      "schedule": "* * * * *"
    }
  ],
  "buildCommand": "echo 'Starting build process' && pip install -r api/requirements.txt --target ./python_packages && echo 'Requirements installed'"
}
